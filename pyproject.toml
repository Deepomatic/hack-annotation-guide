[project]
name = "hack-annotation-guide"
version = "0.1.0"
description = "Generate annotation guide PowerPoint files from Deepomatic Studio projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-pptx>=1.0.2",
    "httpx>=0.27",
    "Pillow>=10.0",
]
